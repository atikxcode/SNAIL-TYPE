1. Check the components folder all the files. Check each file inside of the components folder. Do not skip any file or folder inside of the components folder. 
2. Then check everything inide of the lib folder. 
3. Then check everything inside of the src folder All folders files everything inside of the src folder. 
4. Then read the FULL PROJECT.md file 
5. Then create a new .md file what is remained, what to do everything in that new .md file name that new .md file as workplan.md. You will understand because you already checked everything. 
6. Then start the dev server. 